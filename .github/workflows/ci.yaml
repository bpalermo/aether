name: ci

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

env:
  IMAGE_REGISTRY: index.docker.io/palermo

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Setup Bazel
        uses: bazel-contrib/setup-bazel@0.18.0
        with:
          bazelisk-cache: true
          disk-cache: ${{ github.workflow }}
          repository-cache: true
      - name: Test
        run: |
          bazel test --test_output=errors --test_size_filters=small //...
