name: publish

on:
  push:
    branches:
      - main

env:
  IMAGE_REGISTRY: index.docker.io/palermo

jobs:
  publish:
    runs-on: ubuntu-latest
    environment:
      name: main
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Bazel
        uses: bazel-contrib/setup-bazel@0.14.0
        with:
          bazelisk-cache: true
          disk-cache: ${{ github.workflow }}
          repository-cache: true
      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build
        run: |
          bazel build //...
      - name: Push
        run: |          
          bazel run //agent/cmd/agent:push -- --repository ${{ env.IMAGE_REGISTRY }}/${{ github.event.repository.name }}-agent --tag ${{ github.sha }}
          bazel run //agent/cmd/agent:push -- --repository ${{ env.IMAGE_REGISTRY }}/${{ github.event.repository.name }}-agent --tag latest
